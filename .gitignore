node_modules/
.expo/
dist/
build/

# Native projects are GENERATED (Continuous Native Generation) — `expo prebuild`
# and `expo run:ios` create these. They are build output, not source. Committing
# them means EAS stops regenerating them from app.json/config plugins, and your
# config silently stops taking effect.
/ios
/android

*.orig.*
.DS_Store
*.jks
*.p8
*.p12
*.key
*.mobileprovision
web-build/
dist_check/
dist_check2/
dist-test/
dist_check/
dist_check*
